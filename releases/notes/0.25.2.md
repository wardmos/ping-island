<!-- zh-Hans -->
## 修复
- 修复 Qoder CLI 在 Qoder IDE 终端中启动时点击会错误唤起 Qoder IDE 的问题
- 修复中文输入法组合文字在自定义审批回复中可能提前刷新输入框的问题
- 修复 Qoder CLI `AskUserQuestion` 提交答案后仍可能回落到 CLI 二次审批的问题

## 说明
- Sparkle build 号提升到 `66`，版本号提升到 `0.25.2`
- 本次发布基于最新 `main`，正式发布为 GitHub latest

<!-- en -->
## Fixes
- Fixes Qoder CLI sessions launched inside Qoder IDE terminals opening Qoder IDE when clicked
- Fixes Chinese IME composition being pushed into custom approval replies before the text is committed
- Fixes Qoder CLI `AskUserQuestion` answers still falling back to a second CLI approval after submission

## Notes
- The Sparkle build number is now `66`, and the app version is now `0.25.2`
- This release is based on latest `main` and is published as GitHub latest
