<!-- zh-Hans -->
## 新功能
- 新增 Qoder CN 桌面端和 CLI 支持，适配独立配置目录、启动方式、审批与提问流程
- 为 Qoder CN 桌面端、CLI 和 IDE 扩展设置项加入专属品牌图标

## 修复
- 修复同一工作区同时运行多个 Qwen Code 会话时会话相互替换或消失的问题

## 说明
- Sparkle build 号提升到 `67`，版本号提升到 `0.26.0`
- 本次发布基于最新 `main`，正式发布为 GitHub latest

<!-- en -->
## New
- Adds Qoder CN desktop and CLI support with dedicated configuration, launch, approval, and question flows
- Adds native Qoder CN branding for desktop, CLI, and IDE extension settings

## Fixes
- Fixes concurrent Qwen Code sessions in the same workspace replacing or removing each other

## Notes
- The Sparkle build number is now `67`, and the app version is now `0.26.0`
- This release is based on latest `main` and is published as GitHub latest
