<!-- zh-Hans -->
## 修复
- 修复 Claude Code `AskUserQuestion` 在部分权限路由下被当作普通审批处理的问题
- 修复自定义审批回复输入时可能被焦点刷新打断的问题
- 修复 Qoder CLI 问题回复在 header lookup 路径下不可见的问题
- 修复 cmux Claude Teams 等后台会话启动后 docked Island 通知入口不可见的问题

## 说明
- Sparkle build 号提升到 `65`，版本号提升到 `0.25.1`
- 本次发布基于最新 `main`，正式发布为 GitHub latest

<!-- en -->
## Fixes
- Fixes Claude Code `AskUserQuestion` events being routed like ordinary approvals in some permission paths
- Fixes custom approval reply input losing focus during refreshes
- Fixes Qoder CLI question answers not staying visible on the header lookup path
- Fixes docked Island notification visibility for background sessions such as cmux Claude Teams

## Notes
- The Sparkle build number is now `65`, and the app version is now `0.25.1`
- This release is based on latest `main` and is published as GitHub latest
